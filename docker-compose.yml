services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: warden
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./db/01_schema.sql:/docker-entrypoint-initdb.d/01_schema.sql:ro
      - ./db/02_rls.sql:/docker-entrypoint-initdb.d/02_rls.sql:ro
      - ./db/03_seed.sql:/docker-entrypoint-initdb.d/03_seed.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d warden"]
      interval: 5s
      timeout: 5s
      retries: 10

  keycloak:
    image: quay.io/keycloak/keycloak:26.0
    command: ["start-dev", "--import-realm"]
    environment:
      KC_BOOTSTRAP_ADMIN_USERNAME: admin
      KC_BOOTSTRAP_ADMIN_PASSWORD: admin
      KC_HTTP_ENABLED: "true"
      KC_HOSTNAME_STRICT: "false"
    ports:
      - "8080:8080"
    volumes:
      - ./keycloak/realm-export.json:/opt/keycloak/data/import/realm-export.json:ro
    healthcheck:
      test: ["CMD-SHELL", "exec 3<>/dev/tcp/127.0.0.1/8080 && echo done"]
      interval: 10s
      timeout: 5s
      retries: 20

  jaeger:
    image: jaegertracing/all-in-one:1.60
    environment:
      COLLECTOR_OTLP_ENABLED: "true"
    ports:
      - "16686:16686"
      - "4318:4318"

  mcp-server:
    build: .
    command: ["python", "-m", "warden.server.app"]
    environment:
      WARDEN_PG_DSN: "postgresql://warden_app:app_password@postgres:5432/warden"
      WARDEN_ISSUER: "http://keycloak:8080/realms/warden"
      WARDEN_JWKS_URI: "http://keycloak:8080/realms/warden/protocol/openid-connect/certs"
      WARDEN_RESOURCE_URL: "http://localhost:9000/mcp"
      WARDEN_HOST: "0.0.0.0"
      WARDEN_PORT: "9000"
      WARDEN_NOW: "2026-06-25T12:00:00+00:00"
      OTEL_EXPORTER_OTLP_ENDPOINT: "http://jaeger:4318"
      OTEL_SERVICE_NAME: "warden-mcp"
    ports:
      - "9000:9000"
    depends_on:
      postgres:
        condition: service_healthy
      keycloak:
        condition: service_healthy
      jaeger:
        condition: service_started

  agent:
    build: .
    entrypoint: ["/app/scripts/run_demo.sh"]
    environment:
      WARDEN_ISSUER: "http://keycloak:8080/realms/warden"
      WARDEN_RESOURCE_URL: "http://localhost:9000/mcp"
      WARDEN_MCP_URL: "http://mcp-server:9000/mcp"
      WARDEN_CLIENT_ID: "warden-agent"
      WARDEN_CLIENT_SECRET: "agent-secret"
      KEYCLOAK_TOKEN_URL: "http://keycloak:8080/realms/warden/protocol/openid-connect/token"
      OTEL_EXPORTER_OTLP_ENDPOINT: "http://jaeger:4318"
      OTEL_SERVICE_NAME: "warden-agent"
    depends_on:
      mcp-server:
        condition: service_started
      keycloak:
        condition: service_healthy

volumes:
  pgdata:
